<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    
    <!-- دسترسی‌های اینترنت، سوکت‌ها و مانیتورینگ وضعیت شبکه -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    
    <!-- دسترسی‌های پایداری در پس‌زمینه، قفل پردازنده و ممانعت از به خواب رفتن پروسس‌ها -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- دسترسی‌های سرویس‌های پیش‌زمینه و نوتیفیکیشن متناسب با اندروید ۱۳، ۱۴ و ۱۵ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SYSTEM_EXEMPTED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 
        تنظیمات تگ application:
        ۱. android:label="RedCloud VPN" برای نام رسمی و صحیح برنامه در گوشی
        ۲. android:extractNativeLibs="true" جهت استخراج و اجازه اجرای مستقیم باینری‌های لوکال (Aether و Tor)
        ۳. android:usesCleartextTraffic="true" برای تبادل داده با پورت‌های محلی ساکس ۵ (127.0.0.1)
        ۴. android:enableOnBackInvokedCallback="true" برای انیمیشن‌های بازگشت در اندروید ۱۴+
    -->
    <application
        android:label="RedCloud VPN"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:extractNativeLibs="true"
        android:usesCleartextTraffic="true"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="34">

        <!-- متادیتای توصیف سرویس پیش‌زمینه جهت ممانعت از توقف پروسس‌ها در اندروید ۱۴ و ۱۵ -->
        <property
            android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
            android:value="Continuous VPN and anti-censorship background tunnel protection" />
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ثبت سرویس دائمی ضد بسته شدن (Anti Task-Killer) برای پایداری تمام هسته‌ها -->
        <service
            android:name=".RedCloudCoreService"
            android:enabled="true"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="specialUse|systemExempted"
            tools:targetApi="34" />

        <!-- متادیتای مورد نیاز لایه موتور فلاتر -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    
    <!-- دسترسی‌های سیستمی جهت باز کردن لینک‌های تلگرام، وب‌سایت و ایمیل در اندروید ۱۱ به بالا -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="mailto" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tg" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <category android:name="android.intent.category.DEFAULT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>